# calculates the mean vegetation percentage per type

import pandas as pd
import csv
import matplotlib.pyplot as plt
import numpy as np

df2 = pd.read_csv('spain_wildfire.csv')

column_headers = list(df.columns.values)
print("The Column Header :", column_headers)

df2.replace(np.nan, 0)

per_broadleaved = np.mean(df2['broadleaved_forest_percent'])
print(per_broadleaved)

per_coniferous = np.mean(df2['coniferous_forest_percent'])
print(per_coniferous)

per_mixed = np.mean(df2['mixed_forest_percent'])
print(per_mixed)

per_sclerophillous = np.mean(df2['sclerophillous_vegetation_percent'])
print(per_sclerophillous)

per_transitional = np.mean(df2['transitional_vegetation_percent'])
print(per_transitional)

per_othernat = np.mean(df2['other_natural_percent'])
print(per_othernat)

per_agriculture = np.mean(df2['agriculture_percent'])
print(per_agriculture)

per_artificial = np.mean(df2['artificial_percent'])
print(per_artificial)

per_other = np.mean(df2['other_percent'])
print(per_other)

per_nat2k = np.mean(df2['natura2k_percent'])
print(per_nat2k)
